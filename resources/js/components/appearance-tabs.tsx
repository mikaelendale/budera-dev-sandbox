/**
 * Settings appearance control — same behavior as {@link ModeToggle}.
 * Prefer importing {@link ModeToggle} directly elsewhere.
 */
export { ModeToggle as default } from '@/components/mode-toggle';
